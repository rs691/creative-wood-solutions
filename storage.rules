rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // By default, allow reads and writes only if the user is authenticated.
    // You should customize these rules to be more specific for your app's needs.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
       
        
